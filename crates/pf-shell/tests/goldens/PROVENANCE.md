# Quiet Console approved renders

The PNGs in this directory are copied byte-for-byte from
`pocketforge-os/design/directions/quiet-console/renders/` at design commit
`cb247f7cb7c3a0bc31c9dfb9a08e112917071858`.

They are the approved 1280×720 Home, Library, Detail, Settings, and High Contrast
design references used by the CI mockup-diff ratchet. Update them only when the
approved design changes, and record the new design commit here.
